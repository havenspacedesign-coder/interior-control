# 專案工作規則

## 功能範圍優先原則

當使用者指定只修改某一個功能、頁面或 feature（例如 B1F 商城、施工寶典、首頁、廠商資訊）時，預設採用最小工作範圍：

1. 優先只讀取、搜尋與修改該功能對應的 `app/features/...` 目錄。
2. 可以讀取該功能對應的 `test/...html` 測試頁。
3. 僅在功能確實依賴時，才讀取必要的 `app/shared/` 或其他共用模組。
4. 非必要不得掃描、搜尋、讀取或修改其他七大功能。
5. 非必要不得執行全專案搜尋、全專案測試或全專案檢查。
6. 若單一 feature 內即可完成任務，不得擴大探索範圍。
7. 確實需要跨 feature 才能完成時，才可讀取相關模組，並限制在最低必要範圍。
8. 不得因修改單一功能，而順便整理、重構或修改其他無關功能。

例如使用者說「修改 B1F 商城」時，預設工作範圍為：

- `app/features/b1f/`
- `test/b1f.html`
- B1F 必要的 shared dependencies

除非任務明確需要，否則不讀取或修改：

- `home`
- `project-overview`
- `design-progress`
- `construction-progress`
- `construction-guide`
- `vendors`
- `members`

此規則是本專案後續單一功能修改的預設工作方式，以維持最小工作範圍並避免不必要的全專案探索與 Token 消耗。

## 更新指令約定

當使用者說「以上修改後更新」時，代表已授權依序完成：

1. 完成指定修改與必要的功能範圍驗證。
2. 將本次修改 commit。
3. 直接 push 到 GitHub 的 `main` 分支，更新正式 APP 的部署來源。
